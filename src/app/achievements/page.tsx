import Achievements from "@/components/Achievements";

export default function AchievementsPage() {
    return (
        <main className="flex flex-col min-h-screen bg-[#050505] pt-24">
            <Achievements />
        </main>
    );
}
